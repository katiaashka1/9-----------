
os: linux
sudo: required
language:
- minimal
services:
- docker

jobs:
  include:
    script:
    - docker run -v `pwd`:`pwd` -w `pwd` -t rusdevops/bootstrap-cpp scripts/tests.sh
notifications:
    email: false